public class Rectangle {
    static int length = 2;
    int breadth = 2;

    public void area() {
        int AreaOfRec = length * breadth;
        System.out.println(AreaOfRec);
        int PeriOfRec = 2 * (length + breadth);
        System.out.println(PeriOfRec);
    }
}
